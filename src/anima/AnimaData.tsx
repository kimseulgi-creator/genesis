const firstTitle: string = `황소자리`;
const firstStory: string = `그리스 로마 신화의 디오스쿠로이 형제에 얽힌 신화가 직접적인 모티브가 된 별자리`;
const firstDescription: string = `그리스 로마 신화의 디오스쿠로이 형제에 얽힌 신화가 직접적인 모티브가 된 별자리. '문기둥 별'이라는 별명도 있다.`;
const firstperiod: string = `4.20~5.20`;
const firstseason: string = `봄`;

const secondTitle: string = `전갈자리`;
const secondStory: string = `여름철의 대표적인 별자리 중 하나로, 하지 무렵에 자정에 남중한다. 특유의 거대한 S자 형태와 정중앙에 위치한 진홍색의 1등성 '안타레스'로 유명하다. 점성술에서는 안타레스가 불길한 징조로 여겨져서, 화성이 안타레스에 접근할 때 왕이 궁을 벗어나면 불길한 일이 생긴다고 하여 각별히 조심하도록 했다.`;
const secondDescription: string = `황도 12궁의 제 8궁. 수호성은 화성으로 양자리와 같은 수호성을 갖는다.`;
const secondperiod: string = `10/23~11.22`;
const secondseason: string = `가을`;

export {
  firstTitle,
  firstStory,
  firstDescription,
  firstperiod,
  firstseason,
  secondTitle,
  secondStory,
  secondDescription,
  secondperiod,
  secondseason,
};
